# kubernetes/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: genai-industrial-agent
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: genai-agent
  template:
    metadata:
      labels:
        app: genai-agent
    spec:
      containers:
      - name: agent-container
        image: genai-industrial-agent:latest
        ports:
        - containerPort: 5000
        env:
        - name: GOOGLE_API_KEY
          valueFrom:
            secretKeyRef:
              name: ai-secrets
              key: api-key
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: genai-service
spec:
  selector:
    app: genai-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
